<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.pri.weatherapp.ui.home.HomeFragment"
        android:label="fragment_home"
        tools:layout="@layout/fragment_home" >
        <action
            android:id="@+id/action_homeFragment_to_citySearchDialogFragment"
            app:destination="@id/citySearchDialogFragment" />
    </fragment>
    <dialog
        android:id="@+id/citySearchDialogFragment"
        android:name="com.pri.weatherapp.ui.citysearch.CitySearchDialogFragment"
        android:label="dialog_city_search"
        tools:layout="@layout/dialog_city_search" />
</navigation>